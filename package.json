{
  "name": "musfiq-r-farhan-official",
  "version": "2.0.0",
  "private": true,
  "description": "Official website of Musfiq R. Farhan: static GitHub Pages front end with a Cloudflare Worker (D1 + R2) content API.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "build:fixture": "MRF_EXPORT_FILE=scripts/fixtures/sample-export.json node scripts/build-site.mjs",
    "serve": "npx --yes http-server . -p 4173 -c-1",
    "test": "playwright test",
    "test:routing": "node scripts/test-routing.mjs",
    "test:love-notes": "node scripts/test-love-notes.mjs",
    "verify": "npm run build:fixture && npm run test:routing && npm run test:love-notes && playwright test",
    "test:live": "PLAYWRIGHT_BASE_URL=https://www.musfiqrfarhan.blog playwright test",
    "deploy:worker": "wrangler deploy --config wrangler.toml",
    "migrate": "wrangler d1 migrations apply MRF-API --remote --config wrangler.toml"
  },
  "engines": {
    "node": ">=22"
  }
}
